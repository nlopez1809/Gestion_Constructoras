{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd robles_backend/robles-backend && npm install --omit=dev && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd robles_backend/robles-backend && npx prisma migrate deploy && node src/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
